{
  "name": "data-ingestion-service",
  "version": "1.0.0",
  "description": "Service responsible for ingesting CVs and job descriptions",
  "scripts": {
    "ci:install": "pipenv install",
    "dev": "pipenv run python src/main.py",
    "lint": "pipenv run black .",
    "generate": "pipenv run python3 generate.py",
    "preinstall": "pipenv install --dev"
  },
  "devDependencies": {
    "typescript": "^4.6.2"
  }
}
